
export default function Home() {
  return (
    <main style={{padding: 24, fontFamily: 'system-ui'}}>
      <h1>Crypto Analytics Dashboard</h1>
      <p>Local-first MVP shell. UI talks to FastAPI BFF at <code>/health</code>, <code>/capabilities</code>, etc.</p>
    </main>
  );
}
